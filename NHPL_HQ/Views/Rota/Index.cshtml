@model NHPL_HQ.Models.ShiftRotaVM
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 style="color:white">Rotas</h2>
<h2 style="color:white">@Html.DisplayFor(model => model.Practice.Name)</h2> 
<h2 style="color:white">"Week commencing" @Html.DisplayFor(model => model.Shift.ShiftDate).ToString()</h2>



<table class="table" style="color:white;">
    @* <tr>
          @foreach (var item in Model.ShiftDatesMonday)
            {
                <th style="color:white">
                    @Html.DisplayFor(model => item.Location)
                </th>
               <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
                <th>
                    @Html.DisplayFor(model => model.Shift.Location)
                </th>
            }
        </tr>*@

    <tr>
        <td> </td>
        @foreach (var item in Model.ShiftDatesMonday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
        }
    </tr>
    <tr>
        <td>
            Monday
        </td>
        @foreach (var item in Model.ShiftDatesMonday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
    <tr>
        <td>
            Teusday
        </td>
        @foreach (var item in Model.ShiftDatesTeusday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
    <tr>
        <td>
            Wednesday
        </td>
        @foreach (var item in Model.ShiftDatesWednesday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
    <tr>
        <td>
            Thursday
        </td>
        @foreach (var item in Model.ShiftDatesThursday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
    <tr>
        <td>
            Friday
        </td>
        @foreach (var item in Model.ShiftDatesFriday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
    <tr>
        <td>
            Saturday
        </td>
        @foreach (var item in Model.ShiftDatesSaturday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
    <tr>
        <td>
            Sunday
        </td>
        @foreach (var item in Model.ShiftDatesSunday)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Employee.UserName)
            </td>
        }
    </tr>
</table>


@*<td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>*@

